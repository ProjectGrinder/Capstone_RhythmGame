#include "pch.h"
#include "system.h"

// TEST(Game, change_scene_test)
// {
//     System::Game::change_scene(test_func1);
//     EXPECT_EQ(value, 1) << "Game::change_scene() returned incorrect value(it should be able to call and set outside value ie. impure function)[Expect: 1, Actual: " << value << "]";
//
//     System::Game::change_scene(test_func2);
//     EXPECT_EQ(value, 2) << "Game::change_scene() returned incorrect value(it should be able to call and set outside value ie. impure function)[Expect: 2, Actual: " << value << "]";
//
//     System::Game::change_scene(test_func3);
//     EXPECT_EQ(value, 3) << "Game::change_scene() returned incorrect value(it should be able to call and set outside value ie. impure function)[Expect: 3, Actual: " << value << "]";
// }
